# Prisma build requirement - prevents "Missing DATABASE_URL" error during Vercel builds
# The app uses in-memory mock data at runtime, so this URL is only for prisma CLI
DATABASE_URL="postgresql://localhost:5432/sidequest_build"
